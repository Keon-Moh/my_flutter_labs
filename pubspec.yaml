name: lab_7
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  encrypted_shared_preferences: ^3.0.1
  url_launcher: ^6.3.2
  floor: ^1.4.2 # Downgraded to avoid potential issues

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  floor_generator: ^1.4.2 # Match floor version
  build_runner: ^2.4.12
  # Removed source_gen and analyzer overrides to let floor_generator resolve them

flutter:
  uses-material-design: true
  assets:
    - images/light-bulb.jpg
    - images/question-mark.jpg
    - images/stop-sign.jpg